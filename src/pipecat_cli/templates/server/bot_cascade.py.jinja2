#
# Copyright (c) 2024–2025, Daily
#
# SPDX-License-Identifier: BSD 2-Clause License
#

"""{{ project_name }} - Pipecat Voice Agent

This bot uses a cascade pipeline: Speech-to-Text → LLM → Text-to-Speech

Generated by Pipecat CLI

Required AI services:
{% if stt_service %}
- {{ stt_service.replace('_stt', '').title() }} (Speech-to-Text)
{% endif %}
{% if llm_service %}
- {{ llm_service.replace('_llm', '').title() }} (LLM)
{% endif %}
{% if tts_service %}
- {{ tts_service.replace('_tts', '').title() }} (Text-to-Speech)
{% endif %}

Run the bot using::

    uv run bot.py
"""

{# Import macros for composition #}
{% import 'server/_macros/event_handlers.jinja2' as handlers %}
{% import 'server/_macros/pipeline_components.jinja2' as pipeline %}
{% import 'server/_macros/helper_functions.jinja2' as helpers %}

{# Standard imports #}
{{ imports|join('\n') }}

load_dotenv(override=True)

{% if recording %}
{{ helpers.save_audio_file_function() }}
{% endif %}

{% if 'twilio' in transports %}
{{ helpers.get_call_info_function() }}
{% endif %}

{% if 'daily_pstn_dialout' in transports or 'twilio_daily_sip_dialout' in transports %}
{{ helpers.dialout_manager_class(transports) }}
{% endif %}

async def run_bot(transport: BaseTransport, runner_args: RunnerArguments) -> None:
    """Run the voice bot for this session.

    Args:
        transport: The transport for this session, built by ``create_transport``
            (or by hand for the dial-out/SIP flows).
        runner_args: Runner session arguments. Carries the request ``body``
            (e.g. dial-out settings, SIP call details) and ``session_id``; the
            standard web/telephony pipelines don't need it.
    """
    logger.info("Starting bot")

{% if needs_session %}
    async with aiohttp.ClientSession() as session:
{% set indent_level = 8 %}
{% include 'server/_blocks/run_bot_logic_cascade.jinja2' with context %}
{% else %}
{% set indent_level = 4 %}
{% include 'server/_blocks/run_bot_logic_cascade.jinja2' with context %}
{% endif %}


async def bot(runner_args: RunnerArguments):
    """Main bot entry point."""
    {% if enable_krisp %}
    # Krisp is available when deployed to Pipecat Cloud
    if os.environ.get("ENV") != "local":
        from pipecat.audio.filters.krisp_viva_filter import KrispVivaFilter

        krisp_filter = KrispVivaFilter()
    else:
        krisp_filter = None
    {% endif %}

    {# Bot entry point. Dial-out and SIP keep a bespoke flow (room/token + dial-out
       settings arrive in the request body, built by server.py); everything else —
       including Daily PSTN dial-in — goes through the unified create_transport path. #}
    {% if 'daily_pstn_dialout' in transports %}
    {# Daily PSTN dial-out has its own special flow with server.py. #}
{% include 'server/_blocks/bot_entry_daily_pstn.jinja2' %}
    {% elif 'twilio_daily_sip_dialin' in transports or 'twilio_daily_sip_dialout' in transports %}
    {# Twilio + Daily SIP has its own special flow with server.py, so it doesn't use match/case #}
{% include 'server/_blocks/bot_entry_twilio_daily_sip.jinja2' %}
    {% else %}
    {# Single, unified path: create_transport builds the right transport from the
       runner_args type. You supply only the params; serializers and telephony parsing
       are handled for you. The headless behavioral-eval transport (`uv run bot.py -t
       eval`) is always available too — create_transport supplies its defaults, so it
       needs no entry here. Add an "eval" factory only to customize EvalTransportParams. #}
    transport_params = {
        {% if 'daily' in transports or 'daily_pstn_dialin' in transports %}
        "daily": lambda: DailyParams(
            audio_in_enabled=True,
            {% if enable_krisp %}audio_in_filter=krisp_filter,{% endif %}
            audio_out_enabled=True,
            {% if video_input %}video_in_enabled=True,{% endif %}
            {% if video_output %}
            video_out_enabled=True,
            video_out_is_live=True,
            video_out_width=1024,
            video_out_height=768,
            {% endif %}
        ),
        {% endif %}
        {% if 'smallwebrtc' in transports %}
        "webrtc": lambda: TransportParams(
            audio_in_enabled=True,
            {% if enable_krisp %}audio_in_filter=krisp_filter,{% endif %}
            audio_out_enabled=True,
            {% if video_input %}video_in_enabled=True,{% endif %}
            {% if video_output %}
            video_out_is_live=True,
            video_out_width=1024,
            video_out_height=768,
            {% endif %}
        ),
        {% endif %}
        {% if 'websocket' in transports %}
        "websocket": lambda: FastAPIWebsocketParams(
            audio_in_enabled=True,
            {% if enable_krisp %}audio_in_filter=krisp_filter,{% endif %}
            audio_out_enabled=True,
            add_wav_header=False,
            serializer=ProtobufFrameSerializer(),
        ),
        {% endif %}
        {% for provider in ['twilio', 'telnyx', 'plivo', 'exotel'] if provider in transports %}
        # create_transport sets the {{ provider }} serializer and add_wav_header automatically.
        "{{ provider }}": lambda: FastAPIWebsocketParams(
            audio_in_enabled=True,
            {% if enable_krisp %}audio_in_filter=krisp_filter,{% endif %}
            audio_out_enabled=True,
        ),
        {% endfor %}
    }

    transport = await create_transport(runner_args, transport_params)

    {% if 'twilio' in transports %}
    # OPTIONAL: personalize the call. create_transport already parsed the Twilio
    # websocket, so the call data is on runner_args.call_data (no parse needed). Use
    # the call_id to fetch caller details from the Twilio REST API and inject them.
    # if getattr(runner_args, "call_data", None):
    #     call_info = await get_call_info(runner_args.call_data["call_id"])
    #     if call_info:
    #         logger.info(f"Call from: {call_info.get('from_number')} to: {call_info.get('to_number')}")
    {% elif 'telnyx' in transports %}
    # OPTIONAL: personalize using the pre-parsed call data on runner_args.call_data.
    # if getattr(runner_args, "call_data", None):
    #     logger.info(f"From number: {runner_args.call_data['from']}")
    {% elif 'daily_pstn_dialin' in transports %}
    # OPTIONAL: personalize the call. create_transport already applied the dial-in
    # settings from runner_args.body; the raw webhook payload is still there if you
    # want the caller's number, e.g. to look up a customer record.
    # if isinstance(runner_args.body, dict):
    #     caller = runner_args.body.get("dialin_settings", {}).get("From")
    #     if caller:
    #         logger.info(f"Handling call from: {caller}")
    {% endif %}

    await run_bot(transport, runner_args)
    {% endif %}


if __name__ == "__main__":
    from pipecat.runner.run import main

    main()
